# L3 Router (v1 Skeleton)

Purpose
- Define the Router v1 module boundary and public interfaces.

Scope
- Router actions, state wrapper, receipts, replay hook, and invariant hook.

Non-Scope
- Full routing logic and economic semantics (implemented in later weeks).
- Any account binding semantics.
